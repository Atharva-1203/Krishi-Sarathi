# Dependency Audit Report

- **Backend dependencies (`requirements.txt`)**: Calibrated packages: `fastapi`, `uvicorn`, `scikit-learn`, `shap`, `numpy`, `pandas`.
- **Frontend dependencies (`package.json`)**: Calibrated packages: `next`, `react`, `lucide-react`, `framer-motion`, `zod`, `react-hook-form`.
